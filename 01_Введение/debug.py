first = int(2)
second = int(3)
print(first + second)
# Ошибка: Python воспринимает переменные first и second как строки, а не как числа, поэтому при их сложении получается "23".
# Я преобразовала значения в тип int, чтобы Python выполнил сложение чисел, а не строк. Результат - 5.

age = int(input("Возраст: "))
print(age + 1)
# Ошибка: Введённый возраст "17" является строкой.
# Я использовала int, чтобы преобразовать строку в целое число. Результат - 18.

firstt = 4
secondd = 7
third = 10
average = (firstt + secondd + third) / 3
print(average)
# Ошибка: Без скобок Python сначала выполняет деление, потому что деление имеет более высокий приоритет, чем сложение.
# Я добавила скобки, чтобы сначала сложить все три числа, а затем разделить сумму на 3. Среднее значение равно 7.0.